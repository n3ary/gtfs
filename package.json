{
  "name": "neary-gtfs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GTFS static feed generator for transit agencies (CTP Cluj + extensible)",
  "scripts": {
    "sync": "node src/sync-tranzy.js",
    "build": "node src/build.js",
    "build:agency": "node src/build.js --agency",
    "test": "vitest --run",
    "lint": "eslint src/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
